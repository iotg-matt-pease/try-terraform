# try-terraform

## Quick start

You need to set the following environment variables using your AWS credentials:

```
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
AWS_DEFAULT_REGION

```

For example:

```
export AWS_ACCESS_KEY_ID="anaccesskey"
export AWS_SECRET_ACCESS_KEY="asecretkey"
export AWS_DEFAULT_REGION="us-west-2"
```

To check what terraform will do run:

```
terraform plan
```

To apply to those changes to AWS run (this will cost a small amount of money if you are not within the AWS free tier):

```
terraform apply
```

To remove the changes:

```
terraform destroy
```

